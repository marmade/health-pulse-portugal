# Sessão 14 — 2026-09-15

## Objectivo

Publicar o dashboard fora do Lovable (Crítico nº 5) e abrir a semana que o plano de 14/09
descreve como desbloqueada por esta terça.

Feito. E apareceu, por acidente do caminho, um achado que não estava no plano: **a instância
antiga não está congelada — escreve todos os dias desde Março.**

> **Etiquetas.** `[sessão 14]` = verificado nesta sessão, 15/09/2026, com comando ou leitura
> reproduzível. Segundo eixo: `[bd]`, `[ficheiro]`, `[documento]`, `[painel]`, com
> `[agregado]` como modificador.

---

## 1. O deploy — Cloudflare Pages

**Projecto:** `health-pulse-portugal`, ligado a `marmade/health-pulse-portugal`, ramo de
produção `main`. Preset Vite, `npm run build`, output `dist`. Commit publicado: `87dc741`.

**URL técnico:** `health-pulse-portugal.pages.dev` — a responder.
**Domínio:** `dizquedisse.martamadeira.pt`, registado no painel a 15/09/2026, em propagação.

### A decisão do endereço, e porque não foi a que a Marta pediu primeiro

O pedido inicial era `martamadeira.pt/dizquedisse`. Foi recusado por duas razões, ambas
verificadas antes de propor a alternativa:

- `martamadeira.pt` já é **outro projecto Cloudflare Pages** (`martamadeira`, repo
  `marmade/files`). O Cloudflare não serve dois projectos Pages no mesmo domínio separados
  por pasta sem uma camada extra à frente.
- O dashboard teria de mudar de código: caminho base do Vite e `basename` do router
  (`BrowserRouter`, `src/App.tsx:26`, sem `basename` hoje).

Um subdomínio não custa nada, não toca no código e não tem camada intermédia. **Custo zero
em euros:** subdomínios de um domínio já detido não se pagam, e o plano gratuito do Pages dá
500 builds/mês.

### O primeiro build saiu partido, e isso estava previsto no código

O primeiro deployment correu **sem as variáveis de ambiente** definidas. O build **passou** e
o site abriu **em branco**. É exactamente o comportamento que o `client.ts` documenta desde
09/09/2026: as variáveis são substituídas em tempo de compilação, o build não falha sem
elas, o site é que parte.

Corrigido com `VITE_SUPABASE_URL` e `VITE_SUPABASE_PUBLISHABLE_KEY` em **Production e
Preview**, seguido de *Retry deployment* — as variáveis só entram num build novo.

### O que o site publicado mostra

`[sessão 14][painel]` Cabeçalho: **"ACTUALIZADO 10/08/2026 — 09:27"**. É a data em que a
série de trends parou, e **não** Abril. Confirma no browser o que o Crítico nº 3 já tinha
provado pelo bundle: o site lê `ijpxjpbjudaddfatibfl`.

**Fecha o passo que faltava ao Crítico nº 3** — "falta só abrir no browser", escrito a
09/09/2026 num item já marcado como concluído.

**Por verificar:** a versão de Node que o Cloudflare usou no build. O `.nvmrc` pede 20 e o
build passou, mas **o log do build não foi consultado** — logo não se afirma qual correu.

---

## 2. O achado: dois `pg_cron` vivos na instância antiga

Apareceu ao abrir o painel Cloud do Lovable para o cortar. Evidência completa em
`docs/evidencia/2026-09-15-cron-instancia-antiga/` — três CSV e um README, com os sha256
comparados nas duas pontas.

### 2.1 O "Lovable Cloud" é a instância antiga

`[sessão 14][painel]` + `[bd][agregado]` O painel mostra `historical_snapshots` 12072,
`health_questions` 3362, `news_items` 2128, `bookmarks` 180. Pedidos REST directos a
`cyjwhmuakmiytypewwfw.supabase.co` com a chave `anon` de `scripts/switch_supabase.sh:48`
devolvem **os mesmos quatro números**. São o mesmo sítio.

Consequência prática: o botão `Remove Lovable Cloud` do painel **é** o Crítico nº 4.

### 2.2 Os dois jobs

`[sessão 14][bd]` `select jobid, schedule, command, active from cron.job`, no SQL editor do
Lovable Cloud — único acesso administrativo a esta instância (o MCP responde "You do not
have permission"; a chave `anon` não lê o schema `cron`).

| jobid | active | schedule | invoca |
|---|---|---|---|
| 1 | **true** | `0 6 * * *` (diário) | `fetch-rss-feeds` → `news_items` |
| 2 | false | `0 6 * * *` | `refresh-trends` → `historical_snapshots` |

### 2.3 191 execuções, 191 dias

`[sessão 14][bd][agregado]` `cron.job_run_details` para o jobid 1: primeira execução
registada **09/03/2026 06:00 UTC**, última **15/09/2026 06:00 UTC**, **191 execuções**.

*Aritmética minha:* o intervalo 09/03–15/09 inclusive tem 191 dias. Uma por dia, todos os
dias, sem falhar nenhum. E **156 desses dias são posteriores a 12/04/2026** — a migração
para `ijpxjpbjudaddfatibfl`. O projecto mudou de instância em Abril e este agendador
continuou a correr na antiga durante cinco meses.

As 30 execuções mais recentes (17/08–15/09) estão todas com `status = succeeded`.

**Não se afirma quando os jobs foram criados.** A primeira execução registada é de 09/03,
um dia depois da criação do projecto (08/03/2026, `AUDIT.md` 6.3), mas
`cron.job_run_details` pode ser purgada e isso não foi verificado.

### 2.4 `succeeded` aqui não prova escrita — e é a armadilha conhecida

*Aritmética minha:* cada execução dura ~100 ms (06:00:00.214546 → 06:00:00.313409, a 15/09).
Recolher 44 feeds RSS não se faz em décimo de segundo. O `net.http_post` **despacha** o
pedido e devolve: `succeeded` diz que o pedido saiu, não que a função correu nem que
escreveu.

**É a mesma armadilha do `success` do GitHub Actions**, `AUDIT.md` secções 2 e 3. Duas
ferramentas diferentes, o mesmo erro de leitura: um estado verde que reporta o despacho e é
lido como resultado. O que prova escrita é o crescimento de `news_items` — 1994 a
09/09/2026 (`CONTEXT.md`) contra 2128 a 15/09/2026. *Aritmética minha:* 134 linhas em 6
dias, ~22/dia, compatível com 44 feeds.

### 2.5 A instância nova está limpa

`[sessão 14][bd]` `select ... from cron.job` contra `ijpxjpbjudaddfatibfl`, por MCP
Supabase: **zero linhas**. Nenhum agendador interno. Toda a automação da instância viva
passa pelo GitHub Actions, que está versionado.

Isto fecha a pergunta que a própria descoberta abriu, e é a diferença que interessa: na
antiga a automação era invisível, na nova é um ficheiro que se lê.

### 2.6 O que isto corrige no `CONTEXT.md`

**"Congelada a 30/04" é falso para `news_items`** e verdadeiro para `historical_snapshots`.
O documento trata a instância antiga como inerte; ela escreve todas as manhãs às 06:00 UTC.

A causa do congelamento dos snapshots também deixa de ser mistério: **o job 2 está
inactivo.** Não é avaria — é um interruptor desligado que ninguém sabia existir.

---

## O que NÃO foi feito

- **O Lovable não foi cortado.** Nem a ligação ao GitHub, nem o Cloud. A sessão parou no
  painel quando o achado apareceu.
- **Nada foi apagado na instância antiga.** Os jobs continuam activos. Apagar destrói a
  prova, e a prova foi guardada primeiro — por isso é que a pasta de evidência existe antes
  da decisão.
- **O `lovable-tagger` não foi removido.** Continua importado no topo do `vite.config.ts`,
  fora da condição de modo; sai com a linha, ao mesmo tempo.
- **O `/admin` não foi decidido.** Ver abaixo.
- **O `CONTEXT.md` não foi tocado nesta janela.** Bloco a aplicar no terminal.

---

## Uma decisão que caducou sem ninguém dar por isso

A decisão de 07/09/2026 sobre a palavra-passe do `/admin` foi: não a mudar, **porque o
painel sai com o Lovable**.

O deploy de hoje publicou o `/admin` (`src/App.tsx:40`) num endereço novo e permanente, com
a password comparada no cliente e servida no bundle. O painel não saiu com o Lovable — o
Lovable é que ainda não saiu, e entretanto o painel ganhou um segundo endereço.

A decisão não estava errada quando foi tomada. Deixou de se aplicar no momento em que o
deploy aconteceu, e nada no documento a ligava a esse momento. **Decidir: retirar a rota, ou
registar que a decisão de 07/09 caducou e porquê.**

---

## Nota de processo — uma regra violada por mim

A regra "git só no terminal, nunca pelo bridge de ficheiros do Cowork, comandos de leitura
incluídos" (`CONTEXT.md`, Padrões estabelecidos) **foi violada no arranque desta sessão**:
corri `git log`, `git status` e `git branch` pelo bridge, antes de ler o próprio ficheiro
que a contém.

Verificado logo a seguir: **não ficou nenhum `.git/*.lock`**. Não houve dano. Fica escrito
porque uma regra que se viola sem consequência visível é uma regra que se volta a violar.

---

## Ficheiros criados — **só os da manhã**

> ⚠️ **Esta lista cobre a janela até às 11:31.** A tarde criou três pastas de evidência e
> arquivo e alterou o `CONTEXT.md`, o `vite.config.ts`, o `src/App.tsx` e o `.gitignore` —
> ver a secção "A tarde" no fim deste ficheiro, que remete para cada uma. A lista não foi
> refeita para não duplicar o que já está lá.

- `docs/evidencia/2026-09-15-cron-instancia-antiga/README.md`
- `docs/evidencia/2026-09-15-cron-instancia-antiga/cron-job-export-2026-09-15.csv`
- `docs/evidencia/2026-09-15-cron-instancia-antiga/cron-job-run-details-2026-09-15.csv`
- `docs/evidencia/2026-09-15-cron-instancia-antiga/cron-job-1-periodo-2026-09-15.csv`
- `docs/sessoes/2026-09-15.md` — este registo

Nenhum ficheiro do repositório foi modificado nesta janela. Sem commits: o commit é do
terminal.

---

## Fica em aberto

- **Crítico nº 5** — cortar as duas ligações do Lovable e remover o `lovable-tagger`.
- **Crítico nº 4** — decidir entre desactivar o job 1 e apagar a instância inteira. O SQL de
  apagar dados pessoais em `docs/operacoes/` continua válido se a decisão for faseada.
- **`/admin`** — a decisão caducada acima.
- **Aplicar ao `CONTEXT.md`:** a correcção de "congelada a 30/04", o deploy e o novo
  domínio, a linha de verificação dos crons, e o Crítico nº 3 com o passo do browser fechado.
- **Versão de Node do build do Cloudflare** — por confirmar no log.

---

# Balanço curto de 15/09/2026 — "o plano ainda serve?"

> Escrito no fim da terça, **não** é o balanço de execução de sexta. São duas perguntas
> diferentes: esta é *o plano ainda descreve a semana que estamos a ter?*; a de sexta é
> *cumpriu-se?*. A de sexta fica onde estava.

## O que estava planeado para hoje, e o que aconteceu

| planeado | resultado |
|---|---|
| Preview local (`npm run dev`) e deploy | **Deploy feito**, mas não por preview local: o Cloudflare publicou directamente do `main` e a verificação foi feita no site publicado. O `npm run dev` nunca chegou a ser usado |
| — | `/admin` removido do bundle, com a password a sair com ele |
| — | Dois `pg_cron` encontrados vivos na instância antiga, documentados e desactivados |
| — | Dados pessoais apagados na instância antiga |
| — | 14 tabelas extraídas e verificadas contra contagens administrativas |
| — | `lovable-tagger` removido |

A terça desbloqueou a semana como o plano previa. O que o plano não previa é que
**mudasse o estado do projecto**, e não só a lista de tarefas.

## O que o plano da semana ainda cobre

- **Quarta 16** — exportar as 3634 linhas de autocomplete (Crítico nº 7): **intacto**. É da
  instância nova, não foi tocado hoje.
- **Quarta 16** — inventário das afirmações do dashboard, feito pela Marta: **intacto, e
  agora com o artefacto certo à frente**. Era essa a razão de o deploy vir primeiro.
- **Quinta 17** — varrimento, cada afirmação contra o dado que a sustenta: **intacto, e
  ganhou material** (secção seguinte).
- **Sexta 18** — decidir correcções e balanço de execução: **intacto**.

Acrescenta-se, para amanhã de manhã: **confirmar pelo efeito que o job 1 parou.** Depois das
06:00 UTC, `news_items` da instância antiga ou fica em 2128 ou sobe ~22. Não precisa do
Lovable.

---

# Material para o varrimento de quinta — a rotulagem das notícias

> **Isto apareceu na conversa do Cowork de 15/09 e não estava em ficheiro nenhum.** Fica
> aqui com números suficientes para quinta funcionar sem a conversa.

## De onde veio

Da pergunta se o corpus de notícias da instância antiga valia como "seis meses de dados
acumulados" para cruzar com o que é pesquisado. A resposta é não, e o porquê aplica-se
também à instância viva.

## Instância antiga (`docs/arquivo/2026-09-15-instancia-antiga/news_items.csv`, 2128 linhas)

`[sessão 14][ficheiro][agregado]` Análise do CSV extraído.

- **0 de 2128 têm `keyword_id`.** A versão da Edge Function que lá corria não associava
  notícia a tema.
- **1232 (57,9%)** vêm de um único *outlet* de notícias gerais, "Notícias ao Minuto".
- `related_term` tem 56 valores distintos, mas os dois primeiros — "perturbação
  obsessivo-compulsiva" (580) e "candidíase" (573) — somam **1153, ou 54,2% do total**.
- Numa amostra aleatória de 12 títulos (semente fixa), **8 não têm relação com saúde**:
  Timor-Leste, Liverpool, 25 de Abril, Capital da Cultura, provedor de Justiça, Rio Ave,
  Hong Kong, "O Futuro da Música".
- Vários títulos estão por limpar, com o invólucro XML à vista: `<![CDATA[…]]>`. **São
  1232 (57,9%)** — *contado no terminal a 15/09/2026*, e o número é **exactamente o mesmo
  que o total de "Notícias ao Minuto"**. Não é um defeito geral de limpeza: é **um único
  *feed* cujo invólucro nunca foi desmontado**. Isso torna-o corrigível num sítio só, e
  distingue-o do problema dos rótulos, que é de método e não de *parsing*.

**Não é um corpus.** É recolha sem filtro, com rótulo clínico atribuído a conteúdo que não o
é, acumulada durante 191 dias sem nunca ser limpa.

## Instância viva (`ijpxjpbjudaddfatibfl`, 310 linhas) — o mesmo defeito, mais misturado

`[sessão 14][bd][agregado]` Consultas por MCP Supabase, 15/09/2026.

| | valor |
|---|---|
| total | 310 |
| **com `keyword_id`** | **310 (100%)** — corrigido face à antiga |
| `source_type = media` | 251 |
| `source_type = institucional` | 56 |
| `source_type = factcheck` | **3** |
| **títulos com `<![CDATA[`** | **113 (36,5%)** |
| *outlets* distintos | 33 |
| `related_term` distintos | 50 |

> **Conferido no terminal a 15/09/2026** (Claude Code), antes do commit: os sete valores
> acima batem com a base. A linha `factcheck` foi **acrescentada** nessa verificação — a
> tabela tinha só `media` e `institucional`, que somam 307 contra um total de 310. Não havia
> `source_type` nulo: o resto é um terceiro valor. Uma coluna que não soma ao total é o tipo
> de coisa que, numa tabela de tese, custa a credibilidade de tudo o que está ao lado.

**A associação foi corrigida; o que ela vale não.** Amostra aleatória de 12 (`order by
random()`, 15/09):

**Bem rotuladas** — "Long covid: novos estudos revelam danos cerebrais persistentes"
(Observador) → `long covid` · "Osteoporose afeta 500 mil portugueses e é subdiagnosticada"
(DN) → `osteoporose` · "Sarampo: surto em Lisboa preocupa autoridades de saúde" (SIC) →
`sarampo surto` · "Burnout: trabalhar demais pode sair caro" (Observador) → `burnout` ·
"Microplásticos encontrados no sangue de 80% dos portugueses testados" (Expresso) →
`microplásticos sangue`.

**Mal rotuladas** — "Human Rigths Watch critica condenação de ativistas em Hong Kong"
(Notícias ao Minuto) → **`terapia hormonal`** · "Filme «Banzo» lidera nomeados dos prémios
Sophia" (RTP) → **`síndrome do ovário poliquístico`** · "A epidemia silenciosa das dores da
coluna" (SP Patologia Coluna Vertebral) → **`doenças cardiovasculares`** · "«Vamos ficar de
olhos abertos»: Comissão Europeia promove campanha pelos direitos das…" (APAV) →
**`síndrome do ovário poliquístico`**.

*Ressalva:* 12 é amostra, não medida. **A proporção real de rótulos errados nas 310 não foi
contada.** O que está estabelecido é que o defeito existe na instância viva, não a sua
dimensão.

## Porque é que isto importa para a tese, e não é só um bug

O cruzamento que dá sentido ao lado A é sobrepor três mapas: **o que o Estado nomeia** (as 83
keywords), **o que as pessoas perguntam** (`health_questions`) e **o que os media publicam**
(`news_items`). As zonas onde não coincidem são o objecto do projecto.

O terceiro mapa não está utilizável. E a forma da falha é específica: não faltam notícias —
**o eixo a que cada notícia está associada não é de confiança**. Num cruzamento entre
"notícias sobre menopausa" e "pesquisas sobre menopausa", parte do numerador não é sobre
menopausa. A correlação que saísse não mediria nada, e pareceria um resultado.

Acresce que **a sazonalidade não é alcançável com este material**: precisa de 2+ anos e há
seis meses, dos quais o mais antigo é o arquivo que não serve.

## O que isto dá ao varrimento de quinta

Três afirmações do dashboard que já têm o dado ao lado, prontas a confrontar:

1. **"Estas notícias são deste eixo."** Sustentada por `keyword_id` — presente em 100% das
   linhas e errado numa parte por medir.
2. **Os títulos apresentados ao leitor.** 36,5% trazem `<![CDATA[` visível na página
   publicada.
3. **"ACTUALIZADO 10/08/2026"**, no cabeçalho. É a data em que a série de trends parou, não
   a data de actualização da página. Diz ao leitor uma coisa e mede outra.

**Sugestão de método, não de conclusão:** contar a proporção real de rótulos errados exige
critério humano sobre uma amostra. Cem notícias classificadas à mão dão uma proporção com
intervalo defensável; a impressão de doze não dá.

---

# A tarde — 15/09/2026, das 11:31 em diante

> O registo acima pára às 11:31. A tarde produziu **zero funcionalidade** e foi a mais densa
> do projecto. **Não se repete aqui o conteúdo das pastas de evidência** — cada secção remete
> para onde o trabalho está, com números e scripts reproduzíveis.

## 1. Seis decisões de método sobre a rotulagem

→ `docs/evidencia/2026-09-15-rotulagem-news-items/decisoes-2026-09-15.md`

Fronteiras de palavra; procurar só no título; cortar as sete siglas de três letras;
`depressão` sem regra, com limitação declarada; `ORDER BY` determinista; e a sexta, que saiu
já da classificação — separar o nome do conceito da consulta de pesquisa.

Duas ficaram registadas como **não decididas**: que regra substitui o primeiro-a-casar
quando dois temas casam no mesmo título, e a proporção real de rótulos errados — que nessa
altura ainda não estava contada.

**Nada implementado.** Nenhum ficheiro de código alterado.

## 2. A classificação humana das 100 — e o instrumento que estava mal construído

→ `docs/evidencia/2026-09-15-rotulagem-news-items/classificacao-100-resultados.md`

**O achado sobre o instrumento vale tanto como a medida.** A primeira passagem tinha **uma**
coluna — *"o rótulo está certo?"* — e a Marta respondeu ora a essa pergunta ora a outra,
*"esta notícia pertence ao corpus?"*, conforme a linha. As notas dela provam-no com as
próprias palavras. São **duas variáveis independentes**, uma sobre a máquina e outra
editorial, e a coluna aceitava uma resposta só.

O número da primeira passagem, **38**, não mede nenhuma das duas: é a média de duas
perguntas diferentes. Apresentá-lo como proporção de rótulos errados teria **subestimado o
defeito em 15 pontos**.

Refeito com duas colunas: **rótulo errado 53/100**, **não pertence ao corpus 39/100**, com
intervalos de confiança. As duas passagens ficaram versionadas — a comparação entre elas é
que dá valor ao achado.

**Descobrir isto a usar o instrumento**, e não a desenhá-lo, é o género de coisa que
normalmente não fica registada em lado nenhum.

## 3. O Mural — decisão da Marta de o manter

→ `docs/evidencia/2026-09-15-rotulagem-news-items/pendente-mural.md`, e item próprio nos
Restantes do `CONTEXT.md`.

**O Mural fica.** A objecção inicial da sessão confundia o Mural com o mecanismo que o
alimenta, e foi retirada. O que muda é o **caminho de escrita** do `expandir_mural()`, com
quatro defeitos enumerados — e a ordem: **decidir os quatro antes de religar o HTTP 400**,
porque no minuto em que a função voltar a funcionar escreve com o desenho actual.

## 4. A hipótese do vocabulário separada do defeito de construção da lista

→ `CONTEXT.md`, secção da hipótese do vocabulário.

Era uma coisa e passaram a ser duas, depois de uma verificação que custou minutos: **os
conceitos simples não existem na lista** — nem `mpox`, nem `obesidade`, nem `poluição`, nem
`antibióticos`, nem `gripe`; e `saúde mental` aparece em **8 termos e nunca sozinho**.

- **Defeito de construção** — os termos são consultas de pesquisa e não nomes de conceitos.
  Verifica-se por leitura da lista, não depende de hipótese nenhuma, e é a base da sexta
  decisão.
- **Hipótese do vocabulário** — mantém o que tinha, as 38 de 82 sem resolução no Trends, e o
  teste da reformulação por correr. Do lado das notícias ganha **um caso genuíno**,
  `gripe aviária` contra "gripe das aves".

**As duas partilham a origem — a lista nasceu como sementes do Trends — e partilhar origem
não é confirmar-se uma à outra.**

## 5. A inspecção ao dashboard

→ `docs/evidencia/2026-09-15-dashboard-inspeccao/`

Seguir cada número apresentado ao leitor até à origem. O achado central: **o ranking de
urgência ordena ao contrário do que mostra** — `emergentes` é o último por `avgChange` e o
primeiro por `score`, e a página imprime `-57%` ao lado dele, em primeiro lugar.

Ficou registada uma **divergência sobre a ordem de trabalho** — recolha primeiro contra
rótulos primeiro —, com as duas leituras escritas e **nenhuma decidida**. E o âmbito
declarado: nove páginas e as exportações em PDF **não** foram inspeccionadas.

---

## Registo de processo — três afirmações erradas, a mesma família, apanhadas em horas

**É isto que a sessão deve guardar, mais do que qualquer dos números acima.** Hoje foram
escritas três afirmações falsas em registo factual. As três eram **verificáveis com uma
consulta mais barata do que a afirmação**, e nenhuma delas foi feita antes de escrever.

| # | afirmação | quem a escreveu | quem a apanhou | rasto |
|---|---|---|---|---|
| 1 | *"prova convergente nos dois mapas"* | **Claude Code** | Cowork | commitado, anotado |
| 2 | um valor sobre `sintomas menopausa precoce` | **Cowork** | Cowork | **nenhum** |
| 3 | *"`alertCount` = 0 nos quatro eixos"* | **Cowork** | Claude Code | nunca escrito |

**A família é a mesma — a B do apêndice metodológico:** uma verificação verdadeira, ou uma
memória de uma, colocada a sustentar uma conclusão que ela não sustenta.

1. **A minha.** O mapa das notícias é uma verificação real. Chamar-lhe convergência
   **emprestava à hipótese do vocabulário a força do achado que não precisa dela**.
2. **O valor escrito de memória.** Corrigido antes de ser commitado — e é por isso que
   **não tem rasto no repositório**. O método que saiu daí foi explícito: conferir tudo
   contra a saída do script, não contra a lembrança do que a saída dizia.
3. **O `alertCount`.** Afirmado a partir de uma tabela que o próprio Cowork tinha calculado
   **sem incluir a componente dos alertas** — a coluna não existia, e a ausência foi lida
   como zero. Verificado antes de entrar no documento: **são três alertas**, e são eles que
   levam `emergentes` ao 1.º lugar em vez do 2.º.

**O erro nº 3 tinha a particularidade de fazer o defeito parecer MENOR do que é.** Os erros
que atenuam são mais perigosos do que os que exageram, porque ninguém tem pressa de os
verificar.

### O que muda em relação às semanas anteriores

**O intervalo entre escrever e ser apanhado foi de horas, não de dias.** A contradição da
secção Stack sobreviveu **cinco dias**; o cabeçalho da consolidada, **cinco meses**; o
`pg_cron` da instância antiga, **191 dias**. Hoje: três horas, minutos, e zero.

**Não é por se ter passado a escrever melhor.** É porque cada afirmação passou a ter ao lado
o comando que a produz — e porque as duas janelas leem o trabalho uma da outra em vez de o
assinarem. **As três foram apanhadas por quem não as escreveu, ou por quem voltou a correr o
script.** Nenhuma foi apanhada relendo o texto.

É a regra nº 5 do apêndice a funcionar nos dois sentidos: *um aviso que não se confirma é um
aviso que passa a decidir por nós* — e uma afirmação que não se reconfere é uma afirmação
que passa a ser citada.
