# Sessão 19 — 2026-09-25

## Objectivo

Revisão dos marcos da linha do tempo do `/sobre`, frase a frase, com a Marta. Feita no
**Cowork**: editou só ficheiros em `docs/evidencia/2026-09-18-sobre-linha-do-tempo/marcos/`,
não usou git e não escreveu na base de dados. Fez leituras (SELECT) à base
`ijpxjpbjudaddfatibfl` e ao registo de migrações. O commit e este registo são do Claude
Code, na mesma data.

Excepção, declarada pelo próprio Cowork: no início da sessão correu `git log` e `git status`
(só leitura) noutra pasta, `/Users/marmade/health-pulse-portugal`, uma cópia antiga do
repositório parada em 21/05. Não nesta pasta; nada foi alterado. Contra a regra do
`CONTEXT.md`; fica registado.

Esclarecido pelo Cowork: a pasta que leu no início da sessão estava então em
`/Users/marmade/health-pulse-portugal` e tinha `.git` (último commit 21/05, ficheiros de
28/07). Hoje chama-se `health-pulse-portugal-ANTIGO-nao-usar` e mantém a data de 28/07; sem
`index.lock`. Foi a Marta que lhe mudou o nome e criou a pasta vazia com o nome antigo, às
15:57.

> **Etiquetas.** `[cowork]` = verificado na sessão do Cowork e trazido pela Marta; não
> reverificado pelo Claude Code. `[ficheiro]` = lido no repositório pelo Claude Code, 25/09.

---

## 1. Nove marcos fechados `[ficheiro]`

`README.md` dos marcos actualizado, com o estado de cada um a 25/09. Os nove:
27/03 · 12/04 · 21/05 · 28/07 · 13/08 · 07/09 · 14/09 · 18/09 · 24/09.

## 2. O que mudou em cada marco

**18/09.** Parágrafos da lista e dos alertas escritos, em lista (decisão da Marta). Números
da lista confirmados na base (`keywords`) `[cowork]`. Das 82:

- 60 continuam, 29 delas com outra formulação;
- 2 juntam-se a outras (`alcoolismo` → `dependências`, `prevenção suicídio` → `suicídio`);
- 20 saem;
- entram 40 = 100.

São 29 reformuladas e não 28: `menopausa sintomas` → `sintomas menopausa` não estava contada
na proposta `[cowork]`.

**Anotado, não reescrito:** o "61 / 28 / 21" de `docs/sessoes/2026-09-18.md` §9 e de
`docs/evidencia/2026-09-18-vocabulario/lista-100-proposta.md` conta as duas fusões de forma
inconsistente `[ficheiro]`: a proposta põe `alcoolismo` dentro das 61 ("`dependências` e
`alcoolismo` juntam-se numa linha", l.21–24) e `prevenção suicídio` entre as que saem
("**Saem (13):** … `prevenção suicídio` (→ 13)", l.60–61). A base, lida a 25/09, dá
60 / 2 / 20. Os dois ficheiros ficam como estão: registam o que se contou a 18/09.

Alertas: sem o "109" (decisão de 24/09: vale a contagem do código).

**07/09.** Sai o "3462"; a frase dos 240 valores reescrita pela Marta; Março–Abril passa a
lista; "janela defensável" e "trends" saem.

**14/09.** Sai o "15 pontos"; o parágrafo da classificação reescrito pela Marta, com o
exemplo "Ainda não Chega de hipocrisia?" (Observador, 11/04/2026), rotulado
"perturbação obsessivo-compulsiva" `[ficheiro]`:
`docs/arquivo/2026-09-15-instancia-antiga/news_items.csv` l.468. **Não confirmado** que
estava na amostra de 100: o CSV da amostra não está na pasta.

**21/05.** O "Mudou" e o parágrafo reescritos.

**27/03 e 28/07.** Jargão retirado: "descarga" → "leitura", "corrida" → "recolha",
"trends" → "valores de pesquisa".

**24/09.** Marco novo, "o arquivo repetia a mesma semana"; substitui o 22/09 previsto (a
22/09 não houve sessão). **Rever** depois de segunda 28/09, quando a regra nova do arquivo
for posta à prova, e quando a recolha a partir do servidor estiver feita.

## 3. Verificado no Claude Code, sem corrigir `[claude code]`

Etiqueta nova: `[claude code]` = verificado pelo Claude Code a 25/09, no código, na base
(só SELECT) ou no site publicado. Corrigir fica para outra sessão (decisão da Marta).

### 3a. O bloco de alertas da página inicial continua no site publicado

`Index.tsx` l.390 mostra "Alertas — semana de…" sempre que há `semanaAlertas`; o
`hideAlertas` de 24/09 só está nas colunas (l.252, l.383). Em dizquedisse.martamadeira.pt,
a 25/09, aparece "ALERTAS — SEMANA DE 13 SET". De passagem: o alerta de `creatina` diz
"em curso, 246.ª semana" (não investigado).

**Decisão da Marta, 25/09:** o bloco fica no site por agora, de propósito, como lembrete.
**Sai antes de o site ser mostrado a alguém (orientador, júri).**

### 3b. Os rótulos POC errados: o mecanismo é "época"

A v1 da `fetch-rss-feeds` casava com `lower.includes(kw.toLowerCase())` sobre o título e os
primeiros 200 caracteres da descrição. `perturbação obsessivo-compulsiva` tinha os
sinónimos `["POC", "OCD"]` (`docs/arquivo/2026-09-18-keywords-antes-da-lista-100/`): "POC"
passava a "poc" e casava dentro de qualquer palavra. Os quatro casos de 24/09 §3a:
Juventus ×2 ← "é**poc**a"; Ventura ← "hi**poc**risia"; Pipoca ← "Pi**poc**a".

Na base há 51 notícias com este rótulo (hoje `toc`: a lista de 100 renomeou as linhas).
Pelo título: "época" 28 · "hipocrisia" 5 · "DPOC" 1 · "Pipoca" 1 · "apocalíptico" 1 ·
"hipoclorito" 1 · sem "poc" no título 14. As 14 terão casado na descrição, que a base não
guarda: não verificável.

**O mecanismo principal é "época", não "hipocrisia"** (a hipótese do Cowork estava certa
no mecanismo, e "hipocrisia" é o caso menor). E já estava identificado: o comentário da
v2, escrito a 18/09, diz *"\"POC\" apanhava época"*. O "não foi investigado" de 24/09 §3a
é verdade para essa sessão, mas o achado tinha seis dias.

### 3c. Guiões vazios desde 03/08: falha silenciosa

Os 32 guiões gravados em `guioes_semanais` desde a semana de 03/08 têm **0 perguntas**
cada. O único guião com as 10 perguntas (5 do banco + 5 da IA) é uma linha da semana de 16/03. A função
corre, grava, e o workflow dá sucesso: nada avisa.

**Correcção ao registo de 24/09 §1** (anotada lá, não apagada): a linha
"`guioes_semanais` · 12:42:26 · 33" contou-os como feitos; eram linhas vazias.

### 3d. Os feeds

- **`spesf.pt` continua activa.** A SP Enfermagem de Saúde Familiar (spam de casino em
  finlandês; pendente de 15/09, `CONTEXT.md` l.1254) está na versão publicada da
  `fetch-rss-feeds` (v2, 24/09) e no código local, como `institucional`. A Marta lembra-se
  de ter sido decidido tirá-la; não há registo de que tenha saído. Fonte comprometida activa.
- **41 entradas, 40 feeds.** "Público" e "Público — Ciência" usam o mesmo URL
  (`feeds.feedburner.com/PublicoRSS`). O "42 → 41" fica sem explicação conhecida.

Retirar a `spesf.pt` e publicar a função: decisão da Marta, noutra sessão.

### 3e. Texto do `/sobre` que não é verdade (registar, não corrigir)

No código (`Sobre.tsx`):

| texto | o que é verdade |
|---|---|
| "RSS Feeds · 42 fontes" (l.208, l.290) | 41 entradas, 40 feeds (3d) |
| "Detectar sinais emergentes antes de chegarem aos media" (l.89) | 0 das 100 keywords com `is_emergent`; nada o faz |
| "…em tempo real" (l.84) | tudo corre às segundas, no workflow do GitHub; não há `cron.job` na base |
| "Perplexity Sonar · fontes peer-reviewed" | chama `sonar`; o "peer-reviewed" é um pedido no prompt, não um filtro |
| "Guião · 5 banco + 5 IA" | o código pede 5 + 5; os guiões desde 03/08 estão vazios (3c) |
| "Arquivo · semanal · PDF" | semanal sim; a `archive-weekly` não gera PDF (o PDF é exportado no dashboard) |
| "Google Trends · semanal · pytrends" | pytrends sim; semanal não: o passo está desligado desde 14/08, os lotes são manuais |
| "YouTube · 55 canais" (l.215) | o script tem 56; o `/sobre` diz 48 noutros dois sítios |
| "Autocomplete · semanal" | o passo está desligado no workflow |

Na base (`sobre_conteudo`): "Metodologia", "Fontes de dados" e "Limitações" ainda
descrevem os limiares de 30/50/40 % e os "sinais emergentes >50 %", e dizem que a
sazonalidade não está implementada. Com a linha do tempo na mesma página, contradizem o
marco de 27/03 ("Foram abandonadas").

## 4. A régua da comparação de 15/09 é a de "depressão" `[claude code]`

Pendente de 20/09. Verificado no CSV
(`docs/google-trends/comparacao/2026-09-15-5termos-pesquisa-PT-5a.csv`): máximos menopausa
33 · ansiedade 43 · **depressão 100** · alimentação 52 · obesidade 10.

- **Onde estava o erro:** `docs/metodo/2026-09-18-reguas-e-ancoras.md` §1 ("a régua foi
  fixada pela `ansiedade`"): anotado, a frase fica. E no `esboco.md` dos marcos (l.64): fica
  como está, por ser histórico (decisão da Marta).
- **Onde não estava:** `docs/sessoes/2026-09-18.md` §3 diz só "com quatro termos = 33"; o
  `git log -S` confirma que a frase nunca lá esteve. O registo de 20/09 §2 dizia que sim, e
  o README dos marcos copiou-o: os dois anotados.

## 5. A linha do tempo no `/sobre` `[claude code]`

Commit `f1533ec`: `src/components/LinhaDoTempo.tsx`, duas linhas no `Sobre.tsx`, e as
dependências `react-markdown` + `remark-gfm` (96 pacotes novos no `package-lock.json`,
nenhuma versão existente mudada). Vista e aprovada pela Marta no localhost antes do commit.
**Não está no site publicado** até ao push.

- Lê os nove ficheiros de `marcos/` tal como estão: a página não tem cópia do texto. O
  frontmatter e os comentários HTML não aparecem; `<!-- largura toda -->` põe o parágrafo
  seguinte numa caixa à parte.
- Fica antes de "Limitações", depois de "Como funciona". Desenho do esboço (secção A) e da
  maqueta de 18/09; a introdução é a da maqueta.
- **A régua** vai de uma ponta à outra da secção, com os meses proporcionais aos dias (1/3 a
  1/10). Uma data que não cabe ao lado da anterior desce de fila, medida no próprio ecrã.
  Medido no browser a 768 px (régua de 720 px; entre os pontos de 14/09 e 18/09 ficam 4,5 px)
  e a 1440 px (1392 px): nenhuma sobreposição de pontos, zonas de clique, datas ou meses.
  Na régua a data é o primeiro dia ("07/09"); o intervalo vai por cima do título ("7 a 9 de
  Setembro de 2026").
- Sem nota da pausa de Junho e Julho (decisão de 20/09). No telemóvel, lista vertical por
  mês; o cabeçalho "JUN" fica, sem marcos, como no computador.
- A prosa vai numa caixa `#F5F5FF` (a cor do bloco "Diz que Disse"), com a caixa e o texto à
  largura da secção.
- **As aspas.** Os ficheiros têm aspas direitas; no browser o texto era `U+0022`, sem
  conversão nenhuma. É o `"` da Space Grotesk que se desenha como aspa de fecho: "ansiedade"
  lia-se ”ansiedade”. Passam a “ ” ao mostrar, sem mexer nos ficheiros. Contadas com "Ver
  tudo" nos nove marcos: 60 aberturas, 60 fechos, nenhuma aspa direita a sobrar.
- A nota geral da assistência tem o lugar reservado no fim da secção, vazio: por escrever.

De passagem, não corrigido:
- no marco de 18/09, o "Descobriu-se" dizia *"psicóloga" dá zero com o filtro* e a prosa
  *"psicólogo"*. Entretanto corrigido no ficheiro (18:56, pelo Cowork, a pedido da Marta): as duas
  dizem "psicólogo";
- no telemóvel (570 px), o cabeçalho do site (`EditorialHeader`) sai do ecrã; é anterior à
  secção nova;
- num primeiro carregamento apareceu aberto o 12/04 em vez do 24/09, sem clique; ao
  recarregar, sempre o 24/09. Não reproduzido.

## 6. Decisões da Marta, 25/09

- A régua: o desenho do esboço e da maqueta, sem a pausa, à largura toda, prosa em caixa,
  aspas “ ”, o "JUN" fica no telemóvel. `react-markdown` + `remark-gfm`, sim.
- O bloco de alertas da página inicial fica no site, de propósito, como lembrete; **sai antes
  de o site ser mostrado a alguém (orientador, júri)**.
- Corrigir o texto falso do `/sobre` (§3e), retirar a `spesf.pt` e publicar a
  `fetch-rss-feeds`: noutra sessão.
- O esboço dos marcos fica como está, por ser histórico.
- **A meta muda:** o lançamento em Outubro de 2026 deixa de ser meta. A Marta pediu o
  adiamento da entrega e vai ter de se inscrever no 2.º semestre; data nova por definir.
- O push decide-o a Marta.

## 7. Por fazer

- **Domingo 27/09:** `bash scripts/recolher_domingo.sh`, à mão; ler a última linha
  (SERVE / NÃO SERVE).
- **Segunda 28/09:** confirmar pelo efeito a primeira corrida do arquivo com a regra do
  top 5 (e medir se às 06:00 UTC a semana já vem completa).
- Tirar o bloco de alertas da página inicial (`Index.tsx` l.390) **antes de o site ser
  mostrado a alguém**.
- Escrever a nota geral da assistência, no fim da linha do tempo.
- Rever o marco de 24/09 depois de segunda 28/09, e quando a recolha a partir do servidor
  estiver feita.
- Guiões vazios desde 03/08: perceber porquê (§3c).
- Corrigir o texto do `/sobre` (§3e), no código e em `sobre_conteudo`.
- Retirar a `spesf.pt` da `FEEDS` e publicar a `fetch-rss-feeds` (§3d).
- O alerta de `creatina` "em curso, 246.ª semana" (§3a): não investigado.
