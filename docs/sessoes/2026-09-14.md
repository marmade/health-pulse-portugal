# Sessão 13 — 2026-09-14

## Objectivo
Registar no `CONTEXT.md` o estado verificado a 14/09/2026 — a primeira corrida **agendada**
do pipeline depois de as escritas anónimas terem sido fechadas —, criar `_antigos/` e
arquivar o que já estava decidido. A sessão foi conduzida a partir de um briefing escrito na
janela de estratégia (`docs/operacoes/2026-09-14-briefing-sessao-13.md`), com os pontos de
paragem marcados no próprio briefing.

> **Etiquetas.** `[sessão 13]` = Claude Code, com comando reproduzível, 14/09/2026. As
> leituras da base de dados foram feitas por **MCP Supabase** contra `ijpxjpbjudaddfatibfl`;
> os dados do workflow vieram da **API pública do GitHub**. As linhas novas do `CONTEXT.md`
> já levam o segundo eixo da convenção fechada a 09/09 — `[bd]`, `[ficheiro]`, `[documento]`,
> com `[agregado]` como modificador.

## O arranque: uma divergência de origem, e a sua causa

A sessão começou com um erro que não foi de execução, foi de **origem**. A cópia do
`CONTEXT.md` no Project Knowledge do claude.ai estava na versão de **14/08/2026** enquanto o
ficheiro vivo no repositório ia em **09/09/2026**. Uma sessão leu a cópia como se fosse o
estado actual e trabalhou por cima de um mês de decisões já tomadas.

Isto era exactamente o risco que o padrão "claude.ai não escreve no repositório" registou a
14/08/2026 — "as duas cópias divergirem foi o problema identificado" —, e o padrão descrevia
o risco sem o eliminar: uma cópia só de leitura continua a ser uma cópia, e uma cópia
desactualiza-se sem avisar ninguém.

**Resolvido a 14/09/2026** `[declarado]`: as cópias do `CONTEXT.md` e do `AUDIT.md` foram
retiradas do Project Knowledge e substituídas por **apontadores para o repositório**. Deixou
de haver cópia, e a decisão é que **não se repõe**. Quem precisar do estado vai à fonte.

**As instruções do projecto no claude.ai foram reescritas a 14/09/2026 para apontarem para o
`CONTEXT.md` do repositório.** Elas próprias **vivem na interface do claude.ai e não são
versionáveis** — não há commit que as alcance, nem cópia delas aqui, deliberadamente: uma
cópia seria mais uma coisa a desactualizar-se em silêncio, que é o defeito que esta sessão
corrigiu. A consequência a assumir: **uma divergência entre as instruções e o repositório só
é detectável lendo-as.** Não há diff, não há data, não há aviso. Quem notar que uma sessão do
claude.ai está a trabalhar sobre um estado que não é o do repositório deve ir lê-las primeiro.

> Nota de método: este ponto não foi verificado por mim — foi feito na janela de estratégia e
> transcrito para aqui. O que verifiquei foi a consequência dentro do repositório, na secção
> seguinte, e ela é da mesma família.

## Trabalho realizado

### 1. Seis verificações novas — o que cada uma prova, e o que não prova

Todas re-corridas nesta janela antes de serem escritas, não transcritas do briefing.

**1.1 — O pipeline escreve com `service_role` numa corrida agendada.**
`[sessão 13][bd][agregado]` `count(*)` por tabela nas 19, via `query_to_xml` sobre
`pg_class`, comparado com 07/09 e 09/09 · `[documento]` API do GitHub, `actions/runs`.

Run **#38**, evento `schedule`, 12:18–12:35 UTC, `success`. `news_items` 278→310,
`health_questions` 4604→4647, `guioes_semanais` 25→29, `eixos_archive` 24→28,
`youtube_trends` 22→19 (substituição — o script 4 faz DELETE).

- **Prova:** que o pipeline escreve **sem ninguém carregar no botão**, com zero políticas de
  escrita para `public`/`anon`. O #37 de 09/09 foi `workflow_dispatch`, disparado à mão e
  anterior à migração `20260909190000`; o #38 é a primeira corrida agendada — e a primeira de
  qualquer tipo — depois dela. É a prova que faltava ao Crítico nº 1.
- **Não prova:** que a chave `anon` saiu do pipeline. **Não saiu.** Os passos em Python
  recebem `SUPABASE_SERVICE_ROLE_KEY` do secret, mas os passos 4, 6 e 7 continuam a invocar
  as Edge Functions com `Authorization: Bearer $SUPABASE_ANON_KEY` (`youtube-trends.yml:10`).
  Essas funções escrevem com `service_role` por dentro. O briefing dizia "prova que o pipeline
  não depende da chave anon"; o que ficou provado é que a **escrita** não depende dela.

**1.2 — `historical_snapshots` continua parada por desactivação, não por falha.**
`[sessão 13][bd][agregado]` `count(*)` directo. **3462 linhas, inalteradas** desde 07/09.
Coerente com os passos 1 e 3 comentados a 14/08/2026. Prova que a tabela não está a receber
lixo; não prova nada sobre a qualidade das 3462 linhas que lá estão, que continua má.

**1.3 — Nenhuma política de escrita sobreviveu para `public`/`anon`.**
`[sessão 13][bd]` `pg_policies` filtrado a `cmd <> 'SELECT'`, com a coluna `roles`.
**6 políticas de escrita, todas `{service_role}`**: `app_settings`, `historical_snapshots`,
`news_items`, `plataforma_popups`, `sobre_conteudo`, `trends_cache`. O estado de 09/09
aguentou uma corrida completa do pipeline.

**1.4 — `contactos_projecto` mantém-se fechada.**
`[sessão 13][bd]` `pg_class.relrowsecurity`, contagem de `pg_policies` e `count(*)`.
RLS activo, **0 políticas**, **4 linhas preservadas**. Inalterado desde 09/09.

**1.5 — O agendamento do GitHub chega tarde, e isso não é falha.**
`[sessão 13][documento]` API do GitHub, runs #32–#38. O `cron` pede 06:00 UTC; os arranques
reais foram #32 07:49, #33 07:03, #34 07:06, #35 13:11, #36 12:02, #38 12:18. O atraso do
GitHub em workflows agendados passou de ~1h para ~6h.

- **Não prova** que os passos correram bem. Todas as runs têm conclusão `success`, e **neste
  workflow isso não é prova de escrita**: as falhas dos passos saem como `::warning::`, não
  como erro fatal, e foi assim que os 404 das Edge Functions e os 429 do pytrends passaram
  despercebidos (`AUDIT.md` secções 2 e 3). A frase entrou no `CONTEXT.md` com esta ressalva
  colada — sem ela, dentro de um mês seria lida como "corre sem falhar".

**1.6 — Duas tabelas fora do modelo de dados documentado.**
`[sessão 13][bd]` Listagem de `pg_class` cruzada com a secção "Modelo de Dados"; depois
`grep` por `.from("…")` em `src/`.

- `plataforma_popups`, 15 linhas — **está em uso**: lida por `Plataforma.tsx:91`, escrita
  pelo `/admin` (`Admin.tsx:485` e `:713`). A ausência era do modelo, não da base.
- `trend_data`, **0 linhas** — **não tem um único leitor no `src/`**. A única ocorrência é a
  declaração de tipos gerada em `types.ts:596`, que é ficheiro gerado e não é uma leitura.
  Candidata a remoção; **não removida nesta sessão**.

O modelo passou de 17 para 19 tabelas, com um terceiro grupo para as que não pertencem a
nenhum dos dois lados.

### 2. A contradição dentro do próprio `CONTEXT.md`

A secção **Stack** descrevia, a 14/09, a instância antiga `cyjwhmuakmiytypewwfw` como **"em
uso de facto"** e dizia que o `.env` apontava para lá e não continha credenciais da nova.

Era verdade a 13/08/2026. **Deixou de ser a 09/09/2026** — Crítico nº 3, mesmo documento: o
`.env` foi restaurado do próprio histórico do git, o `npm run build` correu, e o bundle
publicado contém `ijpxjpbjudaddfatibfl` com **zero ocorrências** da instância antiga.

Cinco dias, portanto, em que **o `CONTEXT.md` se contradizia a si próprio**: a tabela de
Verificações e o Crítico nº 3 diziam uma coisa, a secção Stack dizia a contrária. Quem
abrisse pela Stack — que é onde se vai ver "qual é o backend" — saía com o estado errado.

É a lição da sessão 12 outra vez, **a fonte derivada em vez da primária**, desta vez com uma
volta a mais: não foi um documento a contradizer a base de dados, foi **o documento a
contradizer-se dentro de si**. O ficheiro que é suposto ser a fonte de verdade tinha duas
verdades, e a mais visível era a velha.

Corrigido, e o erro ficou registado na própria secção em vez de ser apagado em silêncio.

### 3. A `migration_consolidada.sql` — risco de calendário, não de código

O briefing mandava verificar se a consolidada ainda é o ficheiro de referência para recriar a
instância. **É**, e fica onde está: a secção 5.19 foi reescrita a 09/09 para que reaplicá-la
não reabra a exposição de `contactos_projecto`, e a mesma sessão removeu-lhe 33 blocos de
escrita pública. Arquivá-la deixaria as 38 migrations avulsas como único caminho — incluindo
a `20260320193223`, cujas políticas estão comentadas.

Mas ao lê-la apareceu o que não estava no briefing: a consolidada **recria `trend_data` com
política de leitura pública** (secções 2.3 e 5.3), e o `migrate_data.py:35` lista-a. Se a
instância fosse recriada hoje a partir dela, a tabela vazia voltava, com a política atrás.

**É a segunda vez que este ficheiro reintroduziria uma decisão de segurança já revertida** —
as políticas de `contactos_projecto` a 09/09, o `trend_data` a 14/09.

O ponto não é o `trend_data`, que está vazio e não expõe nada. O ponto é o **calendário**: a
consolidada só se corre numa recriação de emergência, e uma recriação de emergência é
exactamente o dia em que ninguém revê 749 linhas. Um ficheiro que só é lido sob pressão tem de
estar auditado **antes** de a pressão chegar.

Ficou como item próprio em Restantes, e como regra nº 2 do `CLAUDE.md` novo.

> ⚠️ **A afirmação a negrito acima é FALSA, e foi refutada horas depois, no mesmo dia** — ver
> a secção 6.1 da continuação, no fim deste ficheiro, e `AUDIT.md` secção 6. A política de
> leitura pública de `trend_data` **está activa na base de dados e nunca foi revertida**: a
> migração de 09/09 removeu escrita anónima e não tocou em leitura. O parágrafo fica como
> estava porque é o que se pensou nessa altura e o registo não se falsifica — mas não se
> deixa uma afirmação falsa por marcar num ficheiro que outras sessões vão ler.

### 4. `scripts/switch_supabase.sh` — fica, mas só faz metade do que diz

Nada o invoca: nem workflow, nem script, nem `Makefile`. As duas menções são inventários. Pela
letra do critério de arquivo, saía.

Fica, por duas razões que só aparecem ao lê-lo:

- **A função dele não é histórica, é de reparação.** O `.env` foi reescrito pelo bot do
  Lovable a 21/05/2026 (commit `5246597`) no sentido nova→antiga. Enquanto o Lovable estiver
  ligado — Crítico nº 5 por fazer —, isto pode repetir-se, e este script é o que desfaz, com
  backup e diff. Sai na colheita do Lovable, com o `Admin.tsx`.
- **É o único ficheiro versionado fora de documentação que ainda contém
  `cyjwhmuakmiytypewwfw`** — ID, URL e chave anon antiga em claro (l.48). Movê-lo para
  `_antigos/` não removeria nada disso; continuaria versionado, só noutra pasta.

**Mas já só faz metade do que diz, e em silêncio.** Depois de 09/09/2026 os sete scripts
Python leem a chave do ambiente, logo o **passo 5 não encontra nada para substituir** e
reporta `NO CHANGE`. Sobram-lhe três alvos reais: `.env`, `supabase/config.toml` e
`.github/workflows/youtube-trends.yml`. Quem o corresse a seguir a um novo atropelo do Lovable
veria `SWITCH COMPLETE` sem saber o que ficou por trocar — a mesma classe de falha silenciosa
que este projecto passou dois meses a desenterrar. Ficou registado em Restantes.

### 5. `_antigos/` criada, e a pasta órfã arquivada inteira

Primeira mudança para `_antigos/`. Nada apagado: `git mv`, o rasto fica no histórico.

O briefing mandava mover `scripts/.github/workflows/youtube-trends.yml`. Ao olhar, **a pasta
tinha dois ficheiros, não um** — e o segundo não constava da nota do `AUDIT.md` secção 4:

- `youtube-trends.yml` — workflow duplicado e órfão, confirmado a não executar (o GitHub só
  lê `.github/workflows/` na raiz do repositório).
- `7_fetch_autocomplete_questions.py` — **e não é cópia do script activo.** É a versão
  **anterior a 09/09/2026**: tem a chave `anon` escrita no código (l.30) e **não tem a guarda
  `os.environ.get("SUPABASE_SERVICE_ROLE_KEY")`** que faz o script activo parar em voz alta
  quando o secret falta. Correr esta por engano seria correr até ao fim sem gravar nada. É
  **pior** do que a activa, não igual — razão a mais para não ficar ao lado dela.

Moveu-se a pasta inteira. As duas pastas vazias que sobraram foram removidas; `scripts/.github/`
deixou de existir.

**Mover não removeu a chave `anon` do repositório nem do histórico, e não precisava.** A chave
`anon` é pública por desenho e o que fecha o risco é o RLS, fechado a 09/09/2026. Isto foi
higiene, não foi tapar um buraco de segurança — e ficou escrito no corpo do commit para que
ninguém o leia ao contrário daqui a seis meses.

A nota do `AUDIT.md` secção 4 foi actualizada com o novo caminho, o segundo ficheiro e esta
ressalva.

### 6. Um lock do git deixado pelo bridge do Cowork

A meio da sessão o `git add` falhou com `Unable to create '.git/index.lock': File exists`.
Nenhum processo `git` a correr; o único processo com o ficheiro aberto era um processo de
virtualização do macOS, **em leitura** — consequência de o ficheiro existir, não causa. Lock
obsoleto, removido depois de confirmar que o índice estava legível.

**A causa** está no bridge de ficheiros do Cowork: mesmo comandos de leitura como o
`git status` escrevem `.git/index.lock`, e o shell do bridge não tem permissão para o
remover. O mecanismo está provado pelo erro `unable to unlink .git/index.lock: Operation not
permitted` no output dessa sessão; **a correspondência com a hora exacta do lock (15:38) não
foi confirmada** e não se afirma.

Saiu daqui um padrão novo, em "Padrões estabelecidos": **git só no terminal, nunca pelo bridge
de ficheiros do Cowork, comandos de leitura incluídos.** Git é do Claude Code ou da Marta.

### 7. O `Co-Authored-By` que entrou contra a regra — e porque existe agora um `CLAUDE.md`

A regra da Marta é antiga e é clara: **sem `Co-Authored-By` nos commits.** É trabalho de tese,
a autoria é dela, e o registo da assistência fica aqui, em `docs/sessoes/`.

Nesta sessão o **ambiente de sessão injectou uma instrução de atribuição** — mandava terminar
cada commit com `Co-Authored-By` e uma linha de sessão — e essa instrução foi seguida. Os dois
primeiros commits saíram com as linhas lá.

Foram reescritos por `filter-branch` antes de qualquer `push`: hashes novos, conteúdo das
árvores confirmado byte a byte idêntico contra uma tag de salvaguarda, e nenhum ramo remoto os
continha. O push foi *fast-forward*, sem `--force`.

**É esta a razão de existir do `CLAUDE.md` criado hoje.** A regra estava numa memória de
sessão e numa convenção tácita, e uma instrução do ambiente passou-lhe por cima sem
resistência. Agora está no repositório, e a regra diz explicitamente que **vale mesmo quando
uma instrução de sessão mandar o contrário** — porque quem decide a autoria de uma tese é a
autora, não o ambiente que corre a ferramenta.

## As regras ficaram distribuídas por quem as lê

Um ponto de arrumação que vale a pena não perder, porque é contra-intuitivo: **nem todas as
regras do projecto estão no mesmo ficheiro, e isso é deliberado.**

| ficheiro | quem o lê | o que leva |
|---|---|---|
| `CLAUDE.md` | Claude Code | sem atribuição nos commits · auditar a consolidada antes de a reaplicar |
| `CONTEXT.md`, "Padrões estabelecidos" | sessões do Cowork/claude.ai | git só no terminal · claude.ai não escreve no repositório · e o resto |

A regra do git **não** está no `CLAUDE.md`: é dirigida às sessões do Cowork, que leem o
`CONTEXT.md` e não o `CLAUDE.md`. Pôr uma regra no ficheiro que o seu destinatário não abre é
o mesmo que não a escrever. O `CLAUDE.md` tem um cabeçalho a dizer isto, para que ninguém o
leia como se fosse a lista completa.

## O que NÃO foi feito, e porquê

- **Nada foi apagado.** Nem as 3462 linhas de `historical_snapshots`, nem as 3634 de
  autocomplete, nem as 4 de `contactos_projecto`. O que saiu de circulação foi movido.
- **`trend_data` não foi removida.** Está vazia e sem leitores, mas remover obriga a mexer na
  migração `20260308110746`, na consolidada e no `migrate_data.py:35`, e a regenerar o
  `types.ts`. É migração destrutiva e não se faz de passagem. Fica em Restantes.
- **A consolidada não foi auditada.** Foi identificada como risco e ficou o item; a auditoria
  das 749 linhas é trabalho de uma vez e não cabia aqui.
- **A convenção de etiquetas não foi aplicada às ~30 linhas antigas.** As seis novas já a
  levam. As antigas continuam por converter, como desde 09/09.
- **Não se tocou na instância antiga.** Sem acesso administrativo; o SQL está em
  `docs/operacoes/` e quem o corre é a Marta.
- **Não se repuseram políticas de escrita para `public`,** nem se religaram os passos 1 e 3 do
  workflow.

## Lição da sessão: a fonte que se contradiz, e a regra que não chega ao seu leitor

Duas coisas desta sessão são a mesma coisa vista de dois lados.

**A fonte de verdade tinha duas verdades.** O `CONTEXT.md` afirmava, em secções diferentes, que
o site apontava para a instância antiga e que apontava para a nova. Não foi um documento a
ficar para trás da realidade — isso já se sabia tratar, com datas e métodos em cada linha. Foi
um documento a **ficar para trás de si próprio**: uma secção actualizada a 09/09 e outra não,
sem nada que ligasse as duas. A tabela de Verificações protege as linhas que estão nela; não
protege a prosa que as repete noutro sítio do mesmo ficheiro.

**E a regra estava no ficheiro errado.** O `Co-Authored-By` entrou porque a regra vivia numa
memória de sessão — um sítio que uma instrução do ambiente atravessa sem esforço. A cópia do
`CONTEXT.md` no Project Knowledge desactualizou-se porque era uma cópia. Nos dois casos, a
informação existia e não estava onde quem decidia a ia ler.

O que sai daqui, e que orientou as três decisões de arrumação de hoje: **uma regra vale o
ficheiro onde está.** Por isso as cópias no Project Knowledge deram lugar a apontadores, por
isso o `CLAUDE.md` nasceu na raiz do repositório em vez de ficar numa memória, e por isso a
regra do git ficou no `CONTEXT.md` e não no `CLAUDE.md` — cada uma no sítio onde o seu leitor
passa.

## Fica em aberto

- **Auditar a `migration_consolidada.sql`** contra o estado actual da base de dados, antes de
  ser precisa. Item próprio em Restantes.
- **`trend_data`** — remover, sem urgência.
- **Aplicar a convenção de etiquetas** às ~30 linhas antigas da tabela de Verificações.
- **`switch_supabase.sh`** — sai na colheita do Lovable, com o `Admin.tsx` e o
  `lovable-tagger` (Crítico nº 5).
- **Crítico nº 2** — autenticação Supabase a sério, antes de repor escrita no `/admin`.
- **Crítico nº 4** — apagar os dados pessoais da instância antiga e depois a instância.

## Ficheiros criados/modificados

**Criados**
- `CLAUDE.md` — duas regras para o Claude Code, com cabeçalho a dizer onde estão as outras
- `_antigos/` — pasta de arquivo, primeira utilização
- `docs/operacoes/2026-09-14-briefing-sessao-13.md` — o briefing que conduziu a sessão
- `docs/sessoes/2026-09-14.md` — este registo

**Movidos** (`git mv`, renomeação pura, zero alterações de conteúdo)
- `scripts/.github/workflows/youtube-trends.yml` →
  `_antigos/scripts-github-workflows-youtube-trends.yml`
- `scripts/.github/workflows/7_fetch_autocomplete_questions.py` →
  `_antigos/scripts-github-workflows-7_fetch_autocomplete_questions.py`
- A pasta `scripts/.github/` deixou de existir

**Modificados**
- `CONTEXT.md` — cabeçalho a 14/09; 6 linhas de verificação novas; Crítico nº 1 com a prova
  em corrida agendada; Automatização reescrita com a ressalva do `success`; Stack corrigida e
  o erro registado; Modelo de Dados a 19 tabelas com terceiro grupo; 3 itens novos em
  Restantes; 1 padrão novo em Padrões estabelecidos
- `AUDIT.md` — nota da secção 4 com os dois ficheiros, o novo caminho e a ressalva da chave

**Commits**
- `4d7504e` — `docs(context): o estado de 14/09, e uma contradição dentro do próprio ficheiro`
- `e20e4a5` — `chore: cria _antigos/ e arquiva a pasta órfã scripts/.github/`
- `691596d` — `docs: regras onde quem as lê passa, e a consolidada como risco próprio`
- `c414ab9` — `docs(sessao-13): o registo da sessão, e o briefing que a conduziu`
- `ba2c608` — `docs(context): dois pendentes de acção manual, que nenhum commit resolve`
- `bc01d32` — `docs: fecha os dois pendentes manuais — um resolvido, outro escrito`

A lista acima esteve incompleta até à arrumação final — faltavam os três últimos, escritos
depois de ela ter sido redigida. Corrigida no terminal, como a nota da secção seguinte pedia.
O commit que traz esta correcção e a secção 6 do `AUDIT.md` não consta aqui, pela razão
óbvia: é o que está a ser escrito.

Os dois primeiros foram reescritos para retirar linhas de atribuição que o ambiente de sessão
tinha injectado. Hashes anteriores, para o caso de aparecerem nalgum registo: `2227de6` e
`8de6342`. Nenhum dos dois chegou a ir para o remoto.

---

## Continuação, em sessão aberta — auditoria da `migration_consolidada.sql`

> Esta secção foi escrita **durante** a sessão pelo Cowork, à medida que os achados
> apareceram, e não reconstruída no fim a partir da janela de conversa. Motivo: até aqui,
> tudo o que se descobriu viveu horas apenas numa conversa, e teria desaparecido com ela.
> Regra de propriedade acordada a 14/09/2026: durante a sessão o registo é do Cowork, que
> só acrescenta no fim do ficheiro; a arrumação e o commit são do terminal, e a partir da
> passagem o Cowork não volta a tocar.
>
> **Para o terminal:** a lista de commits acima está incompleta — faltam `c414ab9`,
> `ba2c608` e `bc01d32`. Corrigir ao arrumar; não foi corrigido aqui de propósito.

**Fontes.** `[bd]` = instância `ijpxjpbjudaddfatibfl` lida ao vivo por MCP Supabase
(`pg_policies`, `pg_indexes`, `information_schema.columns`, `pg_extension`,
`list_migrations`, `list_tables`). `[ficheiro]` = `scripts/migration_consolidada.sql` no
disco, 632 linhas, 27180 bytes, modificado a 09/09/2026 19:24.

### O que foi refutado

Vale mais do que o que foi confirmado, e é a razão de existir desta subsecção.

- **Hipótese: a consolidada recria o recipiente dos contactos em `revisao_pares`.**
  Refutada. `[bd]` As colunas `telefone_a`, `telefone_b`, `email_a` e `email_b` existem na
  base de dados viva, iguais às do ficheiro. A decisão de 09/09 foi ao nível dos dados
  (esvaziar), não do schema, e o ficheiro é DDL — numa recriação nasceriam vazias. Não há
  regressão aqui.
- **Hipótese: o ficheiro reintroduz a leitura pública de `trend_data` contra uma decisão
  revertida.** Refutada — ver achado 6.1. A política está viva na base neste momento.

### Achados

| # | Achado | Estado |
|---|---|---|
| 6.1 | O pendente acusava o ficheiro de uma regressão que não existe | confirmado |
| 6.2 | O ficheiro afirma ser idempotente e não é | confirmado |
| 6.3 | O histórico de migrações não serve de referência | confirmado |
| 6.4 | Três imprecisões menores (linhas, cabeçalho, nome de política, índice) | confirmado |

**6.1 — a acusação era falsa, e contradizia o ficheiro que a fazia.**
`[bd]` A política `Allow public read on trend_data`, papel `public`, está activa na base de
dados. `[ficheiro]` As secções 2.3 e 5.3 reproduzem-na. Logo o ficheiro descreve o estado
actual e não o contradiz: a migração de 09/09 removeu escrita anónima e nunca tocou em
leitura. Não houve segunda reintrodução de decisão revertida — houve uma, `contactos_projecto`,
tratada a 09/09/2026. O `CONTEXT.md` escrevia as duas coisas a quinze linhas de distância:
que uma tabela vazia com RLS e só leitura pública "não expõe nada" (item do `trend_data`) e
que essa mesma leitura pública era uma decisão de segurança revertida (item da consolidada).
Mesma família da contradição da secção Stack, encontrada mais cedo nesta sessão.

**6.2 — a idempotência é uma afirmação falsa, e falha no dia para que o ficheiro existe.**
`[ficheiro]` Cabeçalho l.5: "single idempotent script". Tabelas, índices e extensões usam
`IF NOT EXISTS`; as 24 `CREATE POLICY` não usam — o PostgreSQL não tem `IF NOT EXISTS` para
políticas — e não existe uma única `DROP POLICY IF EXISTS` no ficheiro. Segunda corrida
sobre uma base que já tenha as políticas aborta em l.429. O cenário real não é a instância
nova e limpa: é a primeira corrida interrompida a meio e a segunda a morrer na primeira
política.

**6.3 — nem a pasta de migrações nem o histórico remoto reconstroem a base que existe.**
`[ficheiro]` 42 ficheiros em `supabase/migrations/`. `[bd]` 8 registos no histórico remoto.
Nenhum dos 38 anteriores a 09/09 aparece no remoto. Os quatro registos de Março no remoto
(`create_base_tables`, `create_dependent_tables`, `fix_eixos_archive_correct_schema`,
`add_eixo_and_subcategoria_to_bookmarks`) não correspondem a nenhum ficheiro local. As quatro
correcções de 09/09 existem nos dois lados com versões diferentes:

| local | remoto |
|---|---|
| `20260909160000_contactos_projecto_rls_restrict.sql` | `20260909151958` |
| `20260909180000_revisao_pares_sem_escrita_anonima.sql` | `20260909180147` |
| `20260909190000_sem_escrita_anonima_em_todas_as_tabelas.sql` | `20260909181907` |
| `20260909200000_revisao_pares_sem_contactos.sql` | `20260909192141` |

*Inferência, não verificada:* o projecto foi criado a 08/03/2026 e o histórico remoto começa
a 25/03 — compatível com o Lovable a aplicar SQL directo sem registar, e com o registo a
começar só quando se passou a aplicar por CLI/MCP. Consequência verificada: um `db push`
desta pasta não vê nenhuma das quatro correcções como aplicada, e a consolidada é na prática
o único caminho de reconstrução que existe. **Isso aumenta a importância dela, não diminui.**

**6.4 — três imprecisões.**
- `[ficheiro]` O ficheiro tem **632 linhas, não 749**. O número errado está no `CONTEXT.md`
  l.649 e no `CLAUDE.md`, ambos escritos nesta sessão. *Inferência:* 749−632 = 117,
  compatível com a substituição das 31 políticas de escrita pela nota de nove linhas a
  09/09 — o que faria do 749 um número pré-09/09 nunca actualizado.
- `[ficheiro]` Cabeçalho: "Generated: 2026-04-12", num ficheiro com notas de 09/09 no corpo
  e modificado a 09/09. O "38 incremental migrations" era exacto a 12/04 (42 locais menos as
  4 de 09/09). Ou seja: o corpo foi actualizado e o cabeçalho não — outra vez o defeito da
  secção Stack.
- Nome de política divergente: `Allow public read on bookmarks` `[ficheiro]` l.565 contra
  `Public read` `[bd]`. E `idx_eixos_archive_axis_week` existe `[bd]` mas não na secção 3
  `[ficheiro]` — logo "recreate the full schema" não é exacto.

### O que confere

`[bd]` + `[ficheiro]`, sem divergência: 19 tabelas contra 19, nomes iguais; 24 políticas
contra 24; **zero políticas de escrita para `public` ou `anon`** em qualquer secção — a
decisão de 09/09 está fielmente reflectida; `contactos_projecto` sem políticas nos dois
lados, com a nota da secção 5.19 a proibir a reposição; colunas de `revisao_pares`,
`contactos_projecto` e `eixos_archive` idênticas; `UNIQUE(axis, week_start)` presente nos
dois lados; `pg_cron` e `pg_net` declarados e instalados.

### Leitura

O ficheiro está em melhor estado do que o pendente que o acusava. A regressão de segurança
anunciada não existe. O que existe são duas afirmações falsas dentro do próprio ficheiro —
a idempotência e a data de geração — e um histórico de migrações que não corresponde a nada.

Para o apêndice metodológico, o achado 6.3 vale mais do que a correcção: o plano de
recuperação assentava num registo de migrações que não descreve a base de dados que existe,
e ninguém mentiu — a ferramenta que construiu o projecto escrevia sem registar. É uma
observação sobre o que acontece à rastreabilidade quando se constrói com ferramentas que
escrevem por nós.

### Em aberto após a auditoria

- [ ] Decisão da Marta: tornar o ficheiro realmente idempotente (`DROP POLICY IF EXISTS`
      antes das 24 `CREATE POLICY`) ou deixar como está e declarar no cabeçalho que corre
      uma só vez, em base vazia. A primeira resolve a re-corrida mas cria uma janela se o
      ficheiro for corrido por engano contra a base viva. **Não decidido a 14/09/2026.**
- [ ] Transcrever estes achados para `AUDIT.md` como secção 6, reescrever o item dos
      Restantes no `CONTEXT.md`, corrigir os dois 749 e o cabeçalho do SQL. Bloco preparado
      na sessão; execução no terminal.

### Teste empírico da consolidada — primeira vez que este ficheiro foi corrido

`[sessão 13][bd]` Feito num projecto Supabase criado para o efeito e descartável:
`teste-consolidada-descartavel`, ref `hypztdsgzuykoksrurto`, eu-west-1, criado a
14/09/2026 14:43 UTC. Organização "Marta Madeira", plano **free**. Custo: **0 USD/mês**
(um *branch* teria custado 0,01344 USD/hora, ~9,68 USD/mês *— aritmética minha*; foi por
isso que não se usou *branch*). A instância real `ijpxjpbjudaddfatibfl` **não foi tocada**:
só consultas de catálogo, as mesmas do resto da sessão.

Nota de método: o ficheiro foi transmitido ao projecto de teste através da janela de
conversa, não copiado byte a byte. A fidelidade da transmissão é atestada pela assinatura
de colunas coincidir com a da instância real — uma só letra trocada mudaria o md5.

**Corrida 1 — ficheiro verbatim, base vazia. Passou.**
As 2 extensões, 19 tabelas, 10 índices, 19 `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` e
24 políticas foram criadas sem erro. `CREATE EXTENSION pg_cron` **não** falhou por
privilégios num projecto novo, ao contrário do que eu admiti como possível. **A promessa da
linha 6 — "run on a fresh Supabase instance to recreate the full schema" — cumpre-se. Era a
primeira vez que estava a ser testada:** o ficheiro foi gerado a 12/04/2026 e nunca tinha
sido corrido.

**Comparação do resultado com a instância real:**

| | teste | real |
|---|---|---|
| tabelas | 19 | 19 |
| políticas | 24 | 24 |
| índices `idx%` | **9** | **10** |
| assinatura de colunas (md5) | `48ba226abfeb347bf2734a123c8944f7` | **igual** |
| assinatura de políticas com nome (md5) | `e6907c4d96fd5dc96087977f6c9b2842` | `1a9a969bc40f5ba54b5041a0707885d0` |
| assinatura de políticas sem nome (md5) | `3cc48946875e9a1cf2f00987e5521cfc` | **igual** |

Leitura das duas divergências, ambas já previstas em 6.4 e agora medidas: o índice a menos
é o `idx_eixos_archive_axis_week`; e as assinaturas de políticas só divergem quando o nome
entra no cálculo — tirando o nome, são idênticas. Ou seja **a única diferença de políticas
entre o ficheiro e a base é o nome de uma delas** (`bookmarks`), não o efeito de nenhuma.

**Corrida 2 — repetição. Falha, com o código de erro à vista.**
`CREATE POLICY "Allow public read on trends_cache"` devolveu
`ERROR: 42710: policy "Allow public read on trends_cache" for table "trends_cache" already
exists`. Como todas as instruções anteriores do ficheiro têm `IF NOT EXISTS` — verificado —
o ponto de morte de uma segunda corrida é a l.429, a primeira política. **6.2 deixa de ser
dedução e passa a observação.**

**Teste da candidata da transacção — e o resultado desmonta-a em parte.**
Duas experiências no projecto de teste, cada uma com um `CREATE TABLE` seguido de um erro
deliberado (`SELECT 1/0`):

| experiência | a tabela sobreviveu? |
|---|---|
| sem `BEGIN` | **não** |
| com `BEGIN` ... `COMMIT` | **não** |

Nos dois casos o total de tabelas ficou em 19. Conclusão: **neste caminho de execução (MCP
`execute_sql`) a protecção já existe sem `BEGIN` nenhum** — o caminho envolve o lote numa
transacção por si. Logo a candidata não acrescenta nada *aqui*.

Mas o que isto revela é mais útil do que a candidata: **a protecção vem de quem corre o
ficheiro, não do ficheiro.** Um `psql` sem `--single-transaction` confirma cada instrução à
medida que avança e deixaria a base a meio; o editor SQL do painel Supabase envolve. Não
testei o caminho `psql` — exige a senha da base do projecto de teste, que não tenho.
**Por verificar.** É esse o argumento a favor de pôr `BEGIN`/`COMMIT` dentro do ficheiro:
não por ser necessário no caminho que testei, mas por tornar a garantia independente de
quem o corre — e no dia da emergência ninguém escolhe o caminho com cuidado.

**Estado do projecto de teste a 14/09/2026:** ainda activo, com o schema criado e as duas
tabelas de teste já revertidas pelos próprios erros. Não há tabela `t_sem_begin` nem
`t_com_begin`. Apagar exige o painel — o MCP do Supabase não oferece eliminação de
projectos, só pausa.

> **Actualização, ainda a 14/09/2026:** o projecto foi **pausado** depois de este parágrafo
> ter sido escrito. "Ainda activo" acima refere-se ao momento do teste. O estado corrente
> está no `CONTEXT.md`, secção Stack, e em `AUDIT.md` 6.5 — **pausado, 0 USD, por apagar
> pelo painel**.

### Segundo teste — o ficheiro **corrigido**, com `BEGIN`/`COMMIT`, corrido de ponta a ponta

`[sessão 13][bd]` Motivo: a Tarefa B alterou o ficheiro e ele **não voltou a ser corrido**.
O cabeçalho passou a afirmar que uma corrida interrompida não deixa rasto, e essa afirmação
era inferência. Pior: se alguma instrução recusasse correr dentro de um bloco de transacção
explícito — a `CREATE EXTENSION pg_cron` era a candidata óbvia — o ficheiro teria ficado com
um cabeçalho novo e um corpo que já não corria. Trocar uma afirmação falsa por outra é o
erro que esta sessão passou o dia a caçar.

**Procedimento.** Projecto `hypztdsgzuykoksrurto` restaurado da pausa; as 19 tabelas
largadas com `CASCADE` e as extensões `pg_cron` e `pg_net` removidas, para reproduzir uma
base genuinamente vazia — confirmado antes de correr: 0 tabelas, 0 políticas, 0 das duas
extensões. Estrutura do ficheiro verificada antes do teste: 660 linhas, sha256
`2b4eb167cbb671cd7df82f731819b43b0e7799c175515223bce079321e241a9a`, `BEGIN` na l.28,
`COMMIT` na l.655, primeira instrução executável na l.38 e última na l.630 — **nenhuma
instrução fora da transacção**. 24 `CREATE POLICY`, 11 instruções de índice.

**Resultado: passou.** Incluindo `CREATE EXTENSION pg_cron` **dentro** de uma transacção
explícita — a hipótese de recusa por bloco de transacção fica **refutada por execução**, e
não por raciocínio.

**E o schema produzido é agora exactamente o da instância real, nas três assinaturas:**

| | teste (ficheiro corrigido) | real |
|---|---|---|
| tabelas | 19 | 19 |
| políticas | 24 | 24 |
| índices `idx%` | **10** | **10** |
| extensões alvo | 2 | 2 |
| assinatura de colunas | `48ba226abfeb347bf2734a123c8944f7` | **igual** |
| assinatura de políticas **com nome** | `1a9a969bc40f5ba54b5041a0707885d0` | **igual** |

A assinatura de políticas com nome era `e6907c4d96fd5dc96087977f6c9b2842` antes da Tarefa B
e passou a coincidir com a da base: **a renomeação da política de `bookmarks` fechou a
última divergência de políticas.** O índice acrescentado nasceu com a definição certa —
`CREATE INDEX idx_eixos_archive_axis_week ON public.eixos_archive USING btree (axis,
week_start DESC)`, idêntica à da instância real.

**Proveniência da definição do índice, que faltava a este registo.** O `(axis, week_start
DESC)` não foi escolha de ninguém: foi lido de `pg_indexes.indexdef` na instância real a
14/09/2026, antes de ser escrito no ficheiro. A omissão foi minha — escrevi o nome do índice
no registo e não a definição, e deixei o terminal a aplicar uma instrução sem lastro. Ele
assinalou-o como não verificável do lado dele, e tinha razão.

**O que continua por verificar, e só isto:** o comportamento pelo caminho `psql` sem
`--single-transaction`. Exige a senha da base, que não tenho. Com o `BEGIN`/`COMMIT` dentro
do ficheiro a questão perde peso — a transacção passou a ser propriedade do ficheiro e não
do executor — mas não desaparece, porque não foi medida.

**Estado do projecto de teste:** voltou a ser pausado depois deste teste, com o schema
dentro. Apagar exige o painel.

**Nota de método, para o apêndice.** Este teste existe porque uma correcção foi aplicada e
dada por boa sem ser corrida. O padrão repetiu-se três vezes num dia — a secção Stack, o
cabeçalho da consolidada, e agora a própria correcção do cabeçalho: **o corpo muda e a
afirmação sobre o corpo fica para trás.** Não é descuido de quem escreve; é o que acontece
quando a afirmação e a coisa afirmada vivem em sítios diferentes.
